<resources>
    <!-- Base theme — Compose renders its own UI, so this is a passthrough to hide the system status bar -->
    <style name="Theme.BudgetTracker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen theme — Deep Navy background with app icon -->
    <style name="Theme.BudgetTracker.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0A2342</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">#0A2342</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.BudgetTracker</item>
    </style>
</resources>
